info:
  name: Post Tag
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/tags
  body:
    mode: json
    json: |
      {
        "name": "Wedding"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
