info:
  name: Start New Scan
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/scan
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests:
  - name: returns 202 Accepted
    assert: res.status == 202
