info:
  name: Patch Image
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8080/api/images/:id
  params:
    - name: id
      value: "1"
      type: path
  body:
    mode: json
    json: |
      {
        "rating": 4,
        "tags": [1, 2]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests:
  - name: returns 200
    assert: res.status == 200
  - name: rating is updated
    assert: res.body.rating == 4
  - name: response has an id
    assert: res.body.id != null
